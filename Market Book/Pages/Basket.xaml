<Page x:Class="Market_Book.Pages.Basket"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Market_Book.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Basket" Name="BasketPage"
      >

    <Grid Background="LightGoldenrodYellow" DataContext="{Binding ElementName=BasketPage, Path=.}">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button Content="В меню" HorizontalAlignment="Right"  VerticalAlignment="Bottom" Height="50" Width="140" Click="Button_Click" Grid.Column="1" Margin="0,0,146,105" Grid.Row="1"/>
        <Label Grid.Row="0" Content="{Binding Book.Name}" VerticalAlignment="Center" FontSize="40" Height="50" Grid.Column="0"/>
        <Label Grid.Row="0" Content="{Binding Book.Name}" VerticalAlignment="Center" FontSize="20" Height="50" Grid.Column="0"/>
    </Grid>
</Page>
